{
  "buildCommand": "bash -lc \"python --version && pip --version && pip install -r requirements.txt && python manage.py check && python manage.py collectstatic --noinput && mkdir -p public/static && cp -a staticfiles/. public/static/\"",
  "builds": [
    {
      "src": "library_management/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "50mb", "runtime": "python3.11.3" }
    }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/public/static/$1" },
    { "src": "/(.*)", "dest": "library_management/wsgi.py" }
  ]
}
