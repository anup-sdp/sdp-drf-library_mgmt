for practice phitron sdp, module 22.5

drf library management system,
cmd project root:
code .
venv with python 3.11:
"C:\Program Files\Python311\python.exe" -m venv venv311
venv311\Scripts\activate  -------

pip install django djangorestframework
pip install django-filter
pip freeze > requirements.txt 

django-admin startproject library_management .
python manage.py startapp library

write all codes.

python manage.py makemigrations
python manage.py migrate

python manage.py createsuperuser
admin-admin@example.com-1234

python manage.py runserver ---------

library > fixtures > initial_dat.json
json to db: 
python manage.py loaddata initial_data.json
python manage.py loaddata data-v2.json
db to json: python manage.py dumpdata library.author library.book library.member --indent 2 > library/fixtures/my_data.json

users:
admin-pw:1234 (role:admin)
anup-barua-pw:aB@12345 (role:member)

whitenoise
python manage.py collectstatic
for vercel deploy, in wsgi.py use app instead of application, 
in settings WSGI_APPLICATION = 'library_management.wsgi.app' # instead of application
also push staticfiles folder to github, vercel was not being able to generate it itself.
